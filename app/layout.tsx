import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "PULS Entertainment - Empowering Artists, Satisfying Consumers",
  description:
    "PULS Entertainment is dedicated to helping artists reach their full potential while making and presenting content and music that satisfies the consumer. Discover our services, talent, and impact in the music industry.",
  keywords:
    "music entertainment, artist development, music production, talent management, entertainment company",
  authors: [{ name: "PULS Entertainment" }],
  viewport: "width=device-width, initial-scale=1",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
